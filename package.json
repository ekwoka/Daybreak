{
  "name": "Daybreak",
  "version": "0.0.10-alpha",
  "description": "Shopify theme built on top of the Dawn theme and extended with TailwindCSS and AlpineJS.",
  "scripts": {
    "serve": "shopify switch --store=the-water-filters-store && shopify theme serve",
    "open": "open http://127.0.0.1:9292/ && open https://the-water-filters-store.myshopify.com/admin/themes/127803883695/editor",
    "reset:assets": "run-s build && run-p css:watch js:watch",
    "build": "run-p build:* && npm run sizes:assets",
    "build:css": "NODE_ENV=production TAILWIND_MODE=build postcss build/tailwind.pre.css --o assets/tailwind.css --verbose",
    "build:critical": "NODE_ENV=critical TAILWIND_MODE=build postcss build/tailwind.critical.pre.css --o snippets/critical-css.liquid --verbose",
    "watch:css": "NODE_ENV=development TAILWIND_MODE=watch postcss build/tailwind.pre.css --o assets/tailwind.css --verbose -w",
    "build:js": "esbuild ./build/build.js --outfile=./assets/daybreak.min.js --platform=browser --target=es2017 --bundle --minify --define:ALPINE_VERSION='\"Daybreak custom\"' && esbuild ./build/pps.js --outfile=./assets/pps.min.js --platform=browser --target=es2017 --bundle --minify",
    "watch:js": "esbuild ./build/build.js --outfile=./assets/daybreak.min.js --platform=browser --target=es2017 --bundle --watch --define:ALPINE_VERSION='\"Daybreak custom\"'",
    "sizes:assets": "ls -lh assets/daybreak.min.js assets/tailwind.css assets/*.comp.css",
    "watch": "run-p serve watch:*",
    "replace:rem": "for f in ./**/*.liquid; do echo $f; perl -pi -e 's/(\\d?\\.?\\d+)(rem)/($1\\/1.6).\"rem\"/eg' \"$f\"; done;",
    "postinstall": "npm run build",
    "update": "ncu -u && rm -rf node_modules && npm i"
  },
  "author": "Eric Kwoka",
  "license": "ISC",
  "dependencies": {
    "@ryangjchandler/fern": "^0.1.0",
    "@tailwindcss/aspect-ratio": "^0.3.0",
    "alpinejs": "^3.4.2"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.3.4",
    "@tailwindcss/typography": "^0.4.1",
    "autoprefixer": "^10.4.0",
    "cssnano": "^5.0.9",
    "esbuild": "^0.13.12",
    "lighthouse": "^8.6.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.3.11",
    "postcss-cli": "^9.0.1",
    "postcss-nesting": "^9.0.0",
    "purgecss": "^4.0.3",
    "tailwindcss": "^2.2.19"
  }
}
